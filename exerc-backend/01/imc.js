// Valores do peso e altura
const peso = 76; // Substitua pelo peso da pessoa em kg
const altura = 1.79; // Substitua pela altura da pessoa em metros

// Cálculo do IMC
const imc = peso / (altura ** 2);

// Impressão do resultado
console.log(`O IMC é: ${imc.toFixed(1)}`);
